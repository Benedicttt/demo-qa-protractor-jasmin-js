stages:
  - build
  - tests
  - cleanup

base image:
  stage: build

  script:
    - docker build --tag application:base --file .docker/base.Dockerfile .

application image:
  stage: build

  script:
    - docker build --tag application:latest --file .docker/base.Dockerfile .